<h1>現在の在庫一覧</h1>
<div class="stocks-container">
  <div class="stocks-search">
    <%= form_with url: stocks_path, method: :get, local: true do %>
      <div class="stock-search-field">
        <%= text_field_tag :q, params[:q], placeholder: "タイトル、現在の在庫数で検索" %>
      </div>
      <%= submit_tag "検索", class: "stock-search-button" %>
    <% end %>
  </div>
  <div class="sort-book-link-list a">
    <%= link_to "在庫数が少ない順", stocks_path(order: :asc, q: params[:q]) %>
    <%= link_to "在庫数が多い順", stocks_path(order: :desc, q: params[:q]) %>
    <%= link_to "作成日時が早い順", stocks_path(order: :desc, q: params[:q]) %>
    <%= link_to "作成日時が遅い順", stocks_path(order: :asc, q: params[:q]) %>
  </div>
</div>
並び替えを調整。作成が遅い順を追加
<div class="total-quantity-parts">件数: <%= Stock.count %></div>
<table class="books-table">
  <thead>
    <tr>
      <th>タイトル</th>
      <th>場所の名前</th>
      <th>現在の数</th>
      <th>移動履歴日時</th>
    </tr>
  </thead>

  <% @stocks.each do |stock| %>
    <tr>
      <td><%= stock.book.title %></td>
      <td><%= stock.location.name %></td>
      <td class="<%= stock_color_class(stock) %>"><%= stock.quantity %></td>
      <td><%= I18n.l(stock.created_at, format: :datetime_jp) %></td>
    </tr>
  <% end %>
</table>
<div class="pagination-link"><%= paginate @stocks %></div>
